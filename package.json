{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@abb/common,@abb/server}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "name": "abb",
  "devDependencies": {
    "lerna": "^3.1.1"
  }
}
